- name: istall nginx
  hosts: web
  become: yes
  tasks:
  - name: command pass
    ansible.builtin.command: id expense
    ignore_errors: true
    register: myexpn

  - name: print
    ansible.builtin.debug:
      msg: "hi"

