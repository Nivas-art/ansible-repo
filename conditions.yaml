- name: istall nginx
  hosts: web
  become: yes
  tasks:
  - name: command pass
    ansible.builtin.command: id expense
    ignore_errors: true
    register: myexpn

  - name: print myexpn
    ansible.builtin.debug:
      msg: "{{myexpn}}"

  
  #- name: command passes
   # ansible.builtin.command: useradd expense
    #when: myexpn !=0

  - name: print
    ansible.builtin.debug:
      msg: "hi"

